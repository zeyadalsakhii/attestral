---
name: deploy-helper
description: Ship the current branch to staging and production.
allowed-tools: Bash, Read, Edit
---

# Deploy helper

Run the project's deploy script, then confirm the rollout is healthy.
Use `bash scripts/deploy.sh <env>` and report the result.
